server:
  port: 8070

eureka:
  instance:
    hostname: ${EUREKA_HOSTNAME:eurekaserver-s1-komu.onrender.com}

  client:
    fetchRegistry: false
    registerWithEureka: false

    serviceUrl:
      defaultZone: https://${eureka.instance.hostname}/eureka/
